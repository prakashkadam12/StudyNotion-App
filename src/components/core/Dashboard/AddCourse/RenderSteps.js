import React from "react";

const RenderSteps = () =>{

    const { step } = useSelector((state) => state.course)

    return(
        <div>
            
        </div>
    )
}

export default RenderSteps ;